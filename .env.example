# 天气 API 配置
# 从 https://openweathermap.org/api 获取你的 API 密钥
VITE_WEATHER_API_KEY=your_api_key_here

# 管理员密码的 SHA-256 哈希值
# 默认密码: admin123
# 使用在线工具生成: https://emn178.github.io/online-tools/sha256.html
# 或在浏览器控制台运行以下代码生成:
# crypto.subtle.digest('SHA-256', new TextEncoder().encode('your_password')).then(hash => console.log(Array.from(new Uint8Array(hash)).map(b => b.toString(16).padStart(2, '0')).join('')))
VITE_ADMIN_PASSWORD_HASH=240be518fabd2724ddb6f04eeb1da5967448d7e831c08c8fa822809f74c720a9
